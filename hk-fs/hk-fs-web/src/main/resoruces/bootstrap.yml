server:
  port: 7130
#  tomcat:
#    basedir: /data/tomcat/tmp
spring:
  application:
    name: hk-fs
  main:
    banner-mode: 'off'
    allow-bean-definition-overriding: true
  cloud:
    config:
      label: "2.1.0.RELEASE"
      profile: dev
      uri: http://127.0.0.1:8770
