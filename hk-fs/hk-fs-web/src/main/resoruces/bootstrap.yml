spring:
  application:
    name: hk-fs-web
  main:
    banner-mode: 'off'
    allow-bean-definition-overriding: true
  cloud:
    config:
      label: "2.1.5.RELEASE"
      profile: dev
      uri: http://127.0.0.1:8770