server:
  port: 8770
spring:
  cloud:
    config:
      server:
        default-application-name: hk-config-server
        git:
          uri: https://github.com/huankai/hk-platform.git
          search-paths: src/main/config
          default-label: "2.0.3.RELEASE"
