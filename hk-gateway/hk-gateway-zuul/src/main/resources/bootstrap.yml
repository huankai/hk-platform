server:
  port: 8771

spring:
  application:
    name: hk-gateway-zuul
  main:
    banner-mode: 'off'
  cloud:
    config:
      label: "2.1.5.RELEASE"
      profile: dev
      uri: http://127.0.0.1:8770