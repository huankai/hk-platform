<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.hk</groupId>
    <artifactId>hk-platform</artifactId>
    <version>2.0.3.RELEASE</version>
    <packaging>pom</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>1.8</java.version>
        <hk.core.version>2.0.3.RELEASE</hk.core.version>
    </properties>

    <modules>
        <module>hk-app</module>
        <module>hk-sso</module>
        <module>hk-gateway</module>
        <module>hk-discovery</module>
        <module>hk-pms</module>
        <module>hk-emi</module>
        <module>hk-fs</module>
        <module>hk-platform-business</module>
        <module>hk-config-server</module>
        <module>hk-message-web</module>
        <module>hk-springboot-admin-server</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.hk</groupId>
                <artifactId>hk-core-spring-boot-dependencies</artifactId>
                <version>${hk.core.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <!-- 注意体会插件配置的顺序，这正体现了一个maven的运行流程 -->
            <plugins>
                <!-- Maven Clean Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>3.0.0</version>
                    <configuration>
                        <skip>false</skip> <!-- 忽略clean [mvn clean -Dmaven.clean.skip=true] -->
                        <failOnError>false</failOnError> <!-- clean时忽略错误 [mvn clean -Dmaven.clean.failOnError=false] -->
                    </configuration>
                    <executions>
                        <execution>
                            <id>auto-clean</id>
                            <phase>clean</phase> <!-- clean生命周期clean阶段 -->
                            <goals>
                                <goal>clean</goal> <!-- 执行clean插件的clean目标 -->
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- compiler插件, 设定JDK版本 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.5.1</version>
                    <configuration>
                        <source>${java.version}</source> <!-- 源代码使用的jdk版本 -->
                        <target>${java.version}</target> <!-- 构建后生成class文件jdk版本 -->
                        <encoding>${project.build.sourceEncoding}</encoding> <!-- 构建使用的编码 -->
                    </configuration>
                </plugin>

                <!-- 运行Maven 单元测试 -->
                <!-- <plugin> <groupId>org.apache.maven.plugins</groupId> <artifactId>maven-surefire-plugin</artifactId>
                <version>2.20.1</version> </plugin> -->

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>2.0.3.RELEASE</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

            </plugins>
        </pluginManagement>
    </build>
</project>