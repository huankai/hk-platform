spring:
  main:
    banner-mode: 'off'
    allow-bean-definition-overriding: true
  application:
    name: hk-flow-web
  cloud:
######################################################### config 配置
    config:
      label: "2.1.5.RELEASE"
      profile: dev
      uri: http://127.0.0.1:8770
flowable:
  app:
    enabled: true
#flowable:
#  database-schema-update: 'false'